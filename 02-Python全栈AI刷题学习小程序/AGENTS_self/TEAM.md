# 水母DIY学习助手 MGX Team

Run mode: `lean`.

Roles: research, product, design, frontend, backend, qa/data, lead. Disable or combine a role only in `manifest.yaml`, preserving its review and veto responsibility.

Each role follows: `Profile → Goal → Inputs → Actions → Skills Router → Debate Rules → Outputs → Handoff → Quality Gate → Constraints → Definition of Done`.

Read `orchestration/WORKFLOW.md`, `COLLABORATION_CONTRACTS.md`, `SKILL_ROUTER.md`, the current run manifest, and upstream handoff before acting. Execute only Actions allowed by the current state.

The user alone approves GATE-1, GATE-2 and GATE-3. Project-specific mutable state lives under `agent-runs/`, never in this team package.


